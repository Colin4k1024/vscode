version: 1
dialect: responses
exchanges:
  - request:
      model: gpt-5.3-codex
      system: ${system}
      messages:
        - role: user
          content: Call customization_elicit_unknown exactly once, then reply with only its exact result.
    response:
      content:
        - type: tool_use
          id: toolcall_0
          name: mcp__elicit_probe_server__customization_elicit_unknown
          input: {}
      stopReason: tool_use
  - request:
      model: gpt-5.3-codex
      system: ${system}
      messages:
        - role: user
          content: Call customization_elicit_unknown exactly once, then reply with only its exact result.
        - role: assistant
          content:
            - type: tool_use
              name: mcp__elicit_probe_server__customization_elicit_unknown
              input: {}
        - role: user
          content:
            - type: tool_result
              tool_use_id: toolcall_0
              content: ELICIT_UNKNOWN:cancel
    response:
      content: ELICIT_UNKNOWN:cancel
      stopReason: end_turn
