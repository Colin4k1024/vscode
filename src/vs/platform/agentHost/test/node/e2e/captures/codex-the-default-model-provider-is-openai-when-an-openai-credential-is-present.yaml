# Hand-written fixture. The `model` fields below are documentary only: replay
# matching projects requests to system+messages (see modelRequestProjection.ts),
# and the wire model is asserted separately by the test.
version: 1
dialect: responses
exchanges:
  - request:
      model: gpt-5.6-sol
      system: ''
      messages:
        - role: user
          content: Reply exactly "default-provider-ok".
    response:
      content: default-provider-ok
      stopReason: end_turn
